import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monochrome.player',
  appName: 'Monochrome Player',
  webDir: 'dist'
};

export default config;
